package fr.papycasu.ssoui;

/**
 * Legacy placeholder kept only for source-tree compatibility.
 *
 * Guacamole 1.6.0+ extensions can expose REST resources directly via
 * AuthenticationProvider#getResource(), so no Guice module is required.
 */
public final class SSOUIModule {

    private SSOUIModule() {
    }
}
